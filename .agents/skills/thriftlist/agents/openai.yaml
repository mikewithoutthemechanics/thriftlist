interface:
  display_name: "Thriftlist"
  short_description: "Repo-specific patterns and workflows for thriftlist"
  default_prompt: "Use the thriftlist repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true